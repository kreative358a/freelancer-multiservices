// l. 470
import { FC, ReactElement } from 'react';

const PageLoader: FC = (): ReactElement => {
  return <div className="w-screen h-screen bg-blue-200 dark:bg-blue-950"></div>;
};

export default PageLoader;
